title = "Data Playground gitleaks config"

# Inherit gitleaks' default rules; only add scoped, documented exceptions below.
[extend]
useDefault = true

[allowlist]
description = "Test suites hardcode obviously-fake credentials (e.g. DP_AUTH_SECRET) as fixtures, not real secrets."
paths = [
    '''kernel/hub/tests/.*''',
]
