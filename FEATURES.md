# Data Playground — Feature Checklist

_Acceptance-review checklist, generated from a codebase-wide survey (each item backed by a `file:line`)._
_Last updated: 2026-07-06. Legend: ✅ implemented · 🟡 partial (see note) · ⬜ not implemented (scaffolded/planned)._

**143 features — ✅ 115 implemented · 🟡 21 partial · ⬜ 7 not implemented.**

| Area | ✅ | 🟡 | ⬜ |
|---|--:|--:|--:|
| Canvas & Node UX | 22 | 3 | 2 |
| Engine & Execution | 16 | 4 | 1 |
| Data, Adapters & Catalog | 25 | 4 | 1 |
| Collaboration, Multi-user & Auth | 20 | 2 | 0 |
| Extensibility & Plugin SPI | 13 | 2 | 1 |
| Platform, Design System & Ops | 19 | 6 | 2 |

## ⚠️ Not-fully-done (focus for acceptance)

- ⬜ **branch / loop / variable control-flow nodes** (Canvas & Node UX) — Accent colors AND the 'control' toolbar category (Toolbar.tsx:12, 'Control flow') exist, but NO node kind is registered with category 'control' anywhere (frontend or kernel/nodespecs.py). The Toolbar filters categories to those with specs (Toolbar.tsx:27), so the Control-flow group never even shows. Loops/branches are instead expressed via the 'section' container's driver script (section.tsx:102). `web/src/theme/tokens.ts:54`
- 🟡 **Section container (nesting / drop-in-drop-out / driver script)** (Canvas & Node UX) — Renders as a sized frame; nodes dropped inside become parentId children via screen-space overlap hit-test (Canvas.tsx:179), draggable back out to detach. Gap: nesting is one level only — a section dropped on a section is explicitly rejected (Canvas.tsx:180). `web/src/nodes/kinds/section.tsx:93`
- 🟡 **Capability-driven viewer tabs** (Canvas & Node UX) — Media capability adds an image grid tab keyed off column capabilities. Only 'media' ships; the vectors tab was deliberately removed (capabilities.tsx:49), so the registry is real but the built-in set is minimal. `web/src/nodes/capabilities.tsx:42`
- 🟡 **Agent dock (build a pipeline from intent)** (Canvas & Node UX) — Plan/Build modes; Build applies the LLM-produced graph (applyAgentGraph) and runs terminals. Gap by design: requires DP_AGENT_MODEL + provider key configured server-side — with no model it is simply 'unavailable' (AgentDock.tsx:32), no rule-based fallback. `web/src/panels/AgentDock.tsx:14`
- ⬜ **Standalone edge delete / reconnect** (Canvas & Node UX) — store.removeEdge exists but is DEAD — no caller anywhere in web/src. deleteKeyCode={null} (Canvas.tsx:292) disables RF's built-in edge delete, there is no onReconnect handler and no edge context menu. A wire can only be removed by deleting one of its nodes; since a new connection into an occupied input is rejected, a single input wire effectively can't be rerouted without deleting the target node. `web/src/store/graph.ts:447`
- 🟡 **Run cancellation + query interruption** (Engine & Execution) — Cancels between steps and interrupts an in-flight DuckDB query on the run's own cursor. Gap (documented db.py:163): a pure-Python runaway inside a transform (e.g. `while True`) can NOT be interrupted in-process — only the subprocess runner's hard kill stops it. `kernel/kernel/plugins/runner.py:268 cancel() sets Event + scope.interrupt(); kernel/kernel/db.py:75 _Scope.interrupt calls con.interrupt() from another thread`
- 🟡 **Content-addressed cache (unchanged plan served from cache)** (Engine & Execution) — In-memory per-process dict only, bounded to last 100 (_MAX_RUNS, runner.py:130) — not persisted across kernel restarts and not shared across web instances/the subprocess runner (SubprocessRunner has no cache). Caches row count + output uri/table, not intermediate relations. `kernel/kernel/plugins/runner.py:76 _plan_hash (node config + source fingerprint + edges/handles); result stored/read at runner.py:141,180; write-skip for idempotent overwrite at runner.py:220`
- 🟡 **Cost / confirm gating** (Engine & Execution) — Gate is a single fixed source-row threshold (5M) — no byte-size/cost model. Unknown size returns needs_confirm=False (runner.py:69), so an uncountable source slips the gate by design (rationale: uncountable→unreadable→fails fast). `kernel/kernel/plugins/runner.py:72 needs_confirm = rows >= _CONFIRM_ROWS (5,000,000); enforced at kernel/kernel/routers/runs.py:168 → HTTP 409 unless req.confirmed`
- ⬜ **Error-rate gate (post-write error threshold)** (Engine & Execution) — Scaffolded but dead: no error-rate is computed or thresholded anywhere (grep for error_rate/threshold finds only the label). The per-transform onError='skip'/'raise' is a separate, real feature. `kernel/kernel/compiler.py:46 emits an '__error_gate__' PlanStep when a write exists, but kernel/kernel/plugins/runner.py:160 handles kind=='error_gate' with only `time.sleep(0.02)``
- 🟡 **Standalone loop node** (Engine & Execution) — A bare 'loop' node performs no actual iteration in the engine — it is a pass-through placeholder. Real iterative control flow is done via `section` (maxRuns driver script). Cycles are rejected up front (must be encapsulated, §5.7, engine.py: is_acyclic checks). `kernel/kernel/executors/engine.py:307 loop/opaque/write just pass through parent on full run and raise NotPreviewable otherwise; compiler maps it to kind='loop' (compiler.py:19)`
- 🟡 **DuckDB adapter — Arrow/Feather/IPC scan** (Data, Adapters & Catalog) — Local files only; feather is read via pyarrow into DuckDB. No object-store path for feather and no append support (write append rejects it, adapters.py:188). `kernel/kernel/plugins/adapters.py:148`
- 🟡 **Write mode append (directory of part files)** (Data, Adapters & Catalog) — Only parquet/csv/tsv (and Lance native append); JSON/Arrow/Feather raise NotImplementedError. Each run adds a part file; reader globs them back (test_kernel.py:386). `kernel/kernel/plugins/adapters.py:185`
- 🟡 **Atomic overwrite (temp file + os.replace)** (Data, Adapters & Catalog) — Atomic only for LOCAL writes — a failed/cancelled write never truncates the existing dataset (test_kernel.py:438). Object-store overwrite writes in place (relies on single-object PUT atomicity; a multi-file/dir object-store overwrite is not transactional). `kernel/kernel/plugins/adapters.py:203`
- 🟡 **Destinations — object-store backend browse + target_uri** (Data, Adapters & Catalog) — Browses a prefix via glob(); mkdir is a no-op for object stores (no real folders — prefix created on write, destinations.py:150). Browse errors surface honestly when creds/bucket missing. `kernel/kernel/destinations.py:61`
- ⬜ **Dataset URI confinement for local paths** (Data, Adapters & Catalog) — GAP: /catalog/register and source nodes accept any absolute local path (abspath+expanduser, no root allowlist); the engine scans it directly (engine.py:158). Only the destinations browse surface is root-confined — dataset READ is not, so an authed user can read arbitrary local files. SSRF over http(s) is blocked, but local-FS confinement is absent. `kernel/kernel/routers/catalog.py:79`
- 🟡 **Collab WebSocket relay (per-canvas room)** (Collaboration, Multi-user & Auth) — Per-instance in-memory only. Multi-instance/stateless-web deployments need sticky routing of each canvas to one instance (acknowledged in main.py:11-13 docstring); peers on different instances will NOT see each other. Fine for the default single-process deployment. `kernel/kernel/main.py:88-135 _collab_rooms is an in-memory dict[canvas_id -> set[WebSocket]]`
- 🟡 **Canvas sharing — visibility (private/workspace) + explicit collaborators (editor/viewer)** (Collaboration, Multi-user & Auth) — Gap: 'workspace' visibility grants EDITOR to every user on the instance (metadb.py:248-249) — there is no workspace read-only tier. Explicit per-user viewer role exists, but you cannot make a canvas workspace-visible read-only. `kernel/kernel/routers/workspace.py:197-213 owner-only add/remove share + set visibility; metadb.canvas_role (metadb.py:240-251); web/src/panels/ShareModal.tsx; test_canvas_sharing_access_and_authz (test_kernel.py:1368)`
- 🟡 **NodeSpec generic frontend rendering (plugin nodes need no frontend code)** (Extensibility & Plugin SPI) — Plugin nodes render generically with typed ports/params from /api/nodes. GAP: NodeParamFields filters out type==='code' params, and GenericNode renders only NodeParamFields — so a plugin node declaring a code param (python/sql) gets NO editor in the generic card; only hand-built cards render code editors. `web/src/nodes/generic.tsx:97-119, generic.tsx:39`
- 🟡 **Capabilities — media/vector column tagging + viewer tabs** (Extensibility & Plugin SPI) — Media/vector detection works (regex over name/type in tag_columns) and the Media viewer grid renders. GAPS: (1) reg.add_capability only announces an id+label to KernelInfo — a plugin CANNOT add new column detection (tag_columns is hardcoded core) nor a viewer tab (that's a separate frontend registration), as the module docstring itself admits (capabilities.py:47-51). (2) The Vector viewer tab was removed (capabilities.tsx:49-50); vectors only get an inline cell chip. `kernel/kernel/plugins/capabilities.py:35-51, web/src/nodes/capabilities.tsx:42-50`
- ⬜ **Pipeline importer SPI (/pipelines/import)** (Extensibility & Plugin SPI) — DEAD SCAFFOLDING. NullImporter exists but is never instantiated; Deps has no `importer` attribute and Registry has no set_importer, so a plugin cannot register an importer. Verified empirically: get_deps().importer raises AttributeError, which the endpoint's generic `except Exception` turns into HTTP 400 ('AttributeError: Deps object has no attribute importer') instead of the intended 501 ImporterNotConfigured. The endpoint is broken for every input. `kernel/kernel/plugins/importer.py:19-27, kernel/kernel/deps.py (no self.importer), kernel/kernel/routers/catalog.py:121-128`
- 🟡 **User-scoped settings** (Platform, Design System & Ops) — DB schema + GET/PUT support scope='user', but SettingsModal only reads/writes global (state seeded from s.global only, line 33). No user-preference UI surface beyond theme (which is localStorage, not the DB). `kernel/kernel/metadb.py:132-140,464-470; routers/workspace.py:246-247`
- 🟡 **shadcn/ui migration** (Platform, Design System & Ops) — Radix+cva shadcn primitives exist and newer panels use them, but the migration is explicitly incomplete — many inline-styled/legacy-var components remain. index.css:4 says 'Preflight is disabled … mid-migration' while tailwind.config.js says 'Preflight ON' — a stale, contradictory comment. `web/src/components/ui/*.tsx (button,dialog,input,select,…); index.css:4 vs tailwind.config.js Preflight comment`
- ⬜ **Agent offline keyword-planner fallback** (Platform, Design System & Ops) — README claims 'Without a configured model it falls back to a built-in offline keyword planner … The dock shows which is active.' The code does the opposite: AgentDock.tsx:13 states 'no rule-based stand-in that pretends to be an LLM' and simply shows 'Agent unavailable'. The advertised offline planner does not exist. `web/src/panels/AgentDock.tsx:13,50-51; README.md:151-152`
- ⬜ **Stateless-web readiness — distributed execution** (Platform, Design System & Ops) — Execution always runs on the accepting instance. reconcile_orphaned_runs assumes a SINGLE execution instance — on startup it marks ALL non-terminal runs failed, so in a multi-instance deploy one instance's restart would cancel another's live runs. Needs per-run instance ownership + heartbeat + an ExecutionBackend plugin. Documented as a known gap. `kernel/kernel/metadb.py:401-419 (reconcile_orphaned_runs TODO); README.md:95-99`
- 🟡 **Realtime collaboration (per-canvas room)** (Platform, Design System & Ops) — In-memory broadcast relay per canvas with presence + role gating (viewer can't relay yjs edits). Last-write-wins, not conflict-free (Yjs/CRDT is dep-present but noted as future hardening). Rooms are process-local → deployment must sticky-route /ws/collab/{canvas_id} to one instance (no server-side coordination). `kernel/kernel/main.py:88-135`
- 🟡 **User management (create users)** (Platform, Design System & Ops) — POST /users exists (gated, no anon self-registration) and GET /users powers the login roster, but there is NO admin UI to create users or set their passwords — must be done via API/script. Only self change-password is surfaced. `kernel/kernel/routers/workspace.py:81-94; web/src/api/client.ts:131`
- 🟡 **Deployment documentation** (Platform, Design System & Ops) — README covers the stateless-web model, DP_DATABASE_URL→Postgres, object storage, and an nginx consistent-hash example for collab. No dedicated deployment guide, no Dockerfile/compose, no reverse-proxy/TLS/systemd runbook. `README.md:77-99 ('Scaling out'); no docs/DEPLOY*.md (docs/ has PRD, TESTING, CONTROL_FLOW only)`
- 🟡 **Pluggable execution backend selection** (Platform, Design System & Ops) — pick_runner honors the 'backend' setting; ships LocalRunner + a real SubprocessRunner. UI runner picker works, but pod/Ray/queue runners are only a documented plugin extension point, not shipped — so 'dedicated execution tier' is scaffolding for now. `kernel/kernel/deps.py:116-147; SettingsModal.tsx:95-104`

---

## Canvas & Node UX

- ✅ **React Flow node canvas** — ReactFlow with dot background, panOnScroll, min/max zoom, fitView; local rfNodes state reconciled from the store to preserve RF-owned measured/width so nodes stay visible.  `web/src/canvas/Canvas.tsx:268`
- ✅ **Uniform node card** — Shared card: accent stripe, status glyph, editable title, type tag, meta line, compact body, and a hover/selection action shelf. All hand-built kinds render through it.  `web/src/nodes/NodeCard.tsx:79`
- ✅ **Node + capability registry (plugin model)** — register(spec,component); index.ts eager-globs kinds/*.tsx so a new kind is just a dropped file. buildNodeTypes derives RF nodeTypes from the registry.  `web/src/nodes/registry.ts:32`
- ✅ **Schema-driven generic nodes (backend/plugin kinds)** — registerGenericNodes() renders any /api/nodes kind with no frontend code — typed ports, param form fields, required-param validation — unless a hand-built card exists.  `web/src/nodes/generic.tsx:97`
- ✅ **Built-in node kinds (source/sample/filter/select/transform/sql/join/aggregate/sort/dedup/write/metric/vector-search/section/note/code)** — 16 hand-built kind files each call register(); backend nodespecs.py mirrors the compute kinds. 'notebook' is migrated to 'transform' on load (graph.ts:239).  `web/src/nodes/kinds/source.tsx:94`
- ⬜ **branch / loop / variable control-flow nodes** — Accent colors AND the 'control' toolbar category (Toolbar.tsx:12, 'Control flow') exist, but NO node kind is registered with category 'control' anywhere (frontend or kernel/nodespecs.py). The Toolbar filters categories to those with specs (Toolbar.tsx:27), so the Control-flow group never even shows. Loops/branches are instead expressed via the 'section' container's driver script (section.tsx:102).  `web/src/theme/tokens.ts:54`
- 🟡 **Section container (nesting / drop-in-drop-out / driver script)** — Renders as a sized frame; nodes dropped inside become parentId children via screen-space overlap hit-test (Canvas.tsx:179), draggable back out to detach. Gap: nesting is one level only — a section dropped on a section is explicitly rejected (Canvas.tsx:180).  `web/src/nodes/kinds/section.tsx:93`
- ✅ **Typed ports + wire types** — Port shape+tint encode wire type (dataset/selection/sample/sql-view/metric/value; tokens.ts:63). Hollow when unconnected, filled when wired, hover grows + shows '+'. Multi-output nodes declare instance ports via config.outputs (registry.ts:54).  `web/src/nodes/Port.tsx:13`
- ✅ **Connect validation (typed, single-input, join two-input)** — isValidConnection checks source wire ∈ target port.accepts (registry.canConnect:89) and rejects an already-occupied input handle; join's a/b handles allow two distinct inputs.  `web/src/canvas/Canvas.tsx:158`
- ✅ **Connect-from-port add-node menu** — A plain click on an output port (Port.tsx:52 dispatches dp-port-click; drag-to-connect suppresses it) opens a menu filtered to kinds whose first input accepts that wire (registry.kindsAcceptingWire), then wires the new node up.  `web/src/canvas/ConnectMenu.tsx:7`
- ✅ **Add-node toolbar (category-grouped)** — Bottom floating toolbar auto-populated from the registry, grouped by category with portal popovers; placed non-overlapping via freePosition. Empty categories are dropped, so 'Control flow' never appears (see branch/loop).  `web/src/canvas/Toolbar.tsx:19`
- ✅ **Selection + marquee + shift/meta multi-select** — selectionOnDrag (left-drag marquee), panOnDrag=[1,2] (middle/right pan); select changes folded into store.selectedIds (Canvas.tsx:144, graph.setSelection:472). Cmd/Ctrl+A selects all.  `web/src/canvas/Canvas.tsx:288`
- ✅ **Node drag with settled-position commit** — Per-frame dragging changes stay local (RF); only the final position (dragging:false) commits to the store as its own undo step, avoiding O(n) doc rewrites and collab-socket floods each mousemove.  `web/src/canvas/Canvas.tsx:128`
- ✅ **Copy / paste / cut / duplicate (single + subgraph)** — cloneSubgraph remaps ids, keeps internal edges, offsets position, resets status to draft. Caveat: in-app module clipboard (graph.ts:78), not the OS clipboard — works within a tab, not across browser tabs/apps.  `web/src/store/graph.ts:476`
- ✅ **Keyboard shortcuts** — Delete/Backspace remove selection; B bypass (honors canBypass); D disable; Cmd/Ctrl+A/C/X/V/D; Cmd+Z / Shift+Z / Cmd+Y undo-redo; Esc closes panels then clears selection. Suppressed inside inputs and over the fullscreen code editor / modals.  `web/src/canvas/Canvas.tsx:216`
- ✅ **Bypass (pass-through)** — Toggles data.bypassed (clears disabled); card shows a dashed accent outline (NodeCard.tsx:100). Gated by spec.canBypass in both the ⋯ menu and the B shortcut.  `web/src/store/graph.ts:535`
- ✅ **Disable + downstream propagation** — disable toggles data.disabled; isDisabled (graph.ts:58) walks upstream so a disabled node turns off its whole downstream branch (dimmed, DISABLED badge on the self-disabled node, run/preview blocked).  `web/src/store/graph.ts:545`
- ✅ **Inline node rename** — Double-click (or single-click when already selected) the title, or ⋯-menu Rename via a dp-rename event; commits on Enter/blur, Esc reverts.  `web/src/nodes/NodeCard.tsx:207`
- ✅ **Per-node action shelf (view data / run / history / code / more)** — Floating shelf on hover / sole-selection / while running: preview (eye), run/stop (non-source), history, edit-code (transform+sql only), and a ⋯ menu (rename/run details/duplicate/bypass/disable/export/lineage/delete). Actions disable with reasons (no source connected / invalid param).  `web/src/nodes/NodeCard.tsx:156`
- ✅ **Node status glyphs (draft/latest/stale/running/failed)** — Status token drives the header glyph+color (NodeCard.tsx:110); editing a node marks it+downstream stale (graph.updateConfig:389), a done run flips to latest and snapshots a version.  `web/src/theme/tokens.ts:74`
- 🟡 **Capability-driven viewer tabs** — Media capability adds an image grid tab keyed off column capabilities. Only 'media' ships; the vectors tab was deliberately removed (capabilities.tsx:49), so the registry is real but the built-in set is minimal.  `web/src/nodes/capabilities.tsx:42`
- ✅ **Schema-aware column-picker fields** — ColumnCombo + useInputColumns feed typed column suggestions into kind cards (e.g. join key, sort/dedup keys) from per-node output schemas fetched on structural changes (graph.refreshSchemas:818).  `web/src/nodes/fields.tsx:1`
- ✅ **Empty-canvas state** — Shown when doc.nodes is empty: 'Add a source' (drops a source at viewport center) and 'Ask the Agent'.  `web/src/canvas/Canvas.tsx:41`
- ✅ **Minimap + zoom controls** — Controls stacked above a pannable MiniMap (kind-accent node colors, click-to-recenter); positioned to not overlap.  `web/src/canvas/Canvas.tsx:296`
- 🟡 **Agent dock (build a pipeline from intent)** — Plan/Build modes; Build applies the LLM-produced graph (applyAgentGraph) and runs terminals. Gap by design: requires DP_AGENT_MODEL + provider key configured server-side — with no model it is simply 'unavailable' (AgentDock.tsx:32), no rule-based fallback.  `web/src/panels/AgentDock.tsx:14`
- ⬜ **Standalone edge delete / reconnect** — store.removeEdge exists but is DEAD — no caller anywhere in web/src. deleteKeyCode={null} (Canvas.tsx:292) disables RF's built-in edge delete, there is no onReconnect handler and no edge context menu. A wire can only be removed by deleting one of its nodes; since a new connection into an occupied input is rejected, a single input wire effectively can't be rerouted without deleting the target node.  `web/src/store/graph.ts:447`
- ✅ **Realtime presence / peer cursors on canvas** — Canvas joins a presence room on mount (Canvas.tsx:82), broadcasts cursor on mousemove (sendCursor), and renders peers' live cursors; graph edits merge via a Yjs CRDT (collab).  `web/src/canvas/PeerCursors.tsx:1`

## Engine & Execution

- ✅ **Lowering engine (graph → DuckDB relation plan)** — Each relational node lowers to a duckdb.DuckDBPyRelation; multi-output nodes lower to {port->Relation} routed by edge source_handle (engine.py:81).  `kernel/kernel/executors/engine.py:150 (LoweringEngine._lower dispatches source/filter/select/sort/dedup/aggregate/sql/join/metric/branch per node type to lazy DuckDB relations)`
- ✅ **Out-of-core execution (DuckDB streaming + disk spill)** — Relational ops stream/spill inside DuckDB natively; Python transforms spill to Parquet under DP_SPILL_DIR and are read back. Spill files GC'd by runner finally (runner.py:192).  `kernel/kernel/plugins/runner.py:142 (full=True LoweringEngine); engine.py:364 _transform_spill streams Python-transform output to temp Parquet with 50k-row flush`
- ✅ **Faithful sample preview (join/sort/vector run full inputs)** — During preview these ops lower their inputs UNSAMPLED (full=True sub-engine) so the preview LIMIT becomes an honest top-N, not a lie over a 2000-row prefix. Preview scan budget is 2000 rows (preview.py:15).  `kernel/kernel/executors/engine.py:118 _faithful_inputs; used by sort (engine.py:220), join (engine.py:261), vector-search (engine.py:423)`
- ✅ **NotPreviewable honesty (P8)** — Cleanly separates 'needs a full pass' from a real error; NOT_PREVIEWABLE_KINDS at engine.py:26, node_previewable() at engine.py:553 honors plugin spec.previewable and transform mode.  `kernel/kernel/executors/engine.py:30 NotPreviewable + raises for aggregate/write/opaque/loop/section/transformed-inputs; preview.py:55 maps it to SampleResult(not_previewable=True) distinct from error`
- ✅ **Local out-of-core runner** — Runs the graph in-process on a background daemon thread; per-node status transitions emitted for live/DB-backed polling.  `kernel/kernel/plugins/runner.py:34 LocalRunner; _execute runs plan steps on a per-run cursor, forces a count for non-sink targets (runner.py:174)`
- ✅ **Isolated subprocess runner** — Real OS-process isolation: crash/OOM/segfault can't take the kernel down; cancel is a hard terminate() (subprocess_runner.py:165). Selectable as backend 'local-subprocess' via Settings (deps.py:135 pick_runner).  `kernel/kernel/subprocess_runner.py:28 SubprocessRunner spawns `python -m kernel.subrun`; kernel/kernel/subrun.py:39 child rebuilds Deps and runs the in-process LocalRunner, atomically writing status JSON`
- 🟡 **Run cancellation + query interruption** — Cancels between steps and interrupts an in-flight DuckDB query on the run's own cursor. Gap (documented db.py:163): a pure-Python runaway inside a transform (e.g. `while True`) can NOT be interrupted in-process — only the subprocess runner's hard kill stops it.  `kernel/kernel/plugins/runner.py:268 cancel() sets Event + scope.interrupt(); kernel/kernel/db.py:75 _Scope.interrupt calls con.interrupt() from another thread`
- ✅ **Per-run DuckDB cursor isolation (db.run_scope)** — Concurrent runs/previews no longer serialize on one global lock, and one run's aborted transaction/view-cleanup can't wedge another. Used by preview (preview.py:37), runner (runner.py:148), schema (schema.py:32), catalog sample.  `kernel/kernel/db.py:84 run_scope() gives each run/preview its own cursor via _base_conn().cursor(); thread-local con at db.py:61; scope exit rolls back + drops only its own views (db.py:106)`
- 🟡 **Content-addressed cache (unchanged plan served from cache)** — In-memory per-process dict only, bounded to last 100 (_MAX_RUNS, runner.py:130) — not persisted across kernel restarts and not shared across web instances/the subprocess runner (SubprocessRunner has no cache). Caches row count + output uri/table, not intermediate relations.  `kernel/kernel/plugins/runner.py:76 _plan_hash (node config + source fingerprint + edges/handles); result stored/read at runner.py:141,180; write-skip for idempotent overwrite at runner.py:220`
- 🟡 **Cost / confirm gating** — Gate is a single fixed source-row threshold (5M) — no byte-size/cost model. Unknown size returns needs_confirm=False (runner.py:69), so an uncountable source slips the gate by design (rationale: uncountable→unreadable→fails fast).  `kernel/kernel/plugins/runner.py:72 needs_confirm = rows >= _CONFIRM_ROWS (5,000,000); enforced at kernel/kernel/routers/runs.py:168 → HTTP 409 unless req.confirmed`
- ✅ **Run estimates** — Deliberately coarse and honest — no fabricated per-op ETA (documented runner.py:63). Reports 'size unknown' when no source is countable.  `kernel/kernel/plugins/runner.py:62 estimate() reports real source-row count + step count + placement; kernel/kernel/routers/runs.py:127 _row_estimate counts the largest feeding source`
- ✅ **Transform escape hatch (Python over Arrow RecordBatches)** — onError='skip' drops failed rows/batches (engine.py:479); non-previewable modes (e.g. flat_map_generator not in PREVIEWABLE_MODES) refuse on sample. Full run spills to Parquet.  `kernel/kernel/executors/engine.py:331 _transform; modes map/filter/flat_map/map_batches applied in _apply_fn (engine.py:472); library-processor path at engine.py:333`
- ✅ **Soft sandbox for user cell code** — Explicitly a SOFT guard, not a security boundary (docstring sandbox.py:1) — CPython exec is not isolable; org deployments need OS-level isolation. Blocks the __class__/__subclasses__ escape and '__'-containing string literals.  `kernel/kernel/sandbox.py:82 compile_operator with builtins whitelist (sandbox.py:29), import allow-list (sandbox.py:16), AST dunder rejection (sandbox.py:58), wall-clock timeout (sandbox.py:109)`
- ✅ **Vector search (Lance native ANN + brute-force cosine fallback)** — Query vector from explicit config or a chosen row; runs over the FULL input even in preview (faithful). Native ANN only when input is a bare Lance source with no ops between (engine.py:459).  `kernel/kernel/executors/engine.py:415 _vector_search; native path via adapter.nearest for a bare .lance source (engine.py:447), else list_cosine_similarity brute-force scan (engine.py:454)`
- ✅ **Section meta-programming (driver-script composite node)** — Not sample-previewable (full pass only, engine.py:187). Per-iteration results materialized to Parquet and GC'd via shared spill list. Same soft sandbox as transform — a script that never calls run() (e.g. `while True`) is not time-bounded (section.py docstring).  `kernel/kernel/section.py:75 run_section — a Python driver script calls contained nodes by alias with run()/value()/concat()/emit(), maxRuns cap (section.py:87), multi-output ports`
- ✅ **Per-node output schema (typed vs untyped ports)** — Powers editor column suggestions. Runs on its own cursor, no timeout (a timeout would abandon a lock-holding worker).  `kernel/kernel/executors/schema.py:20 schema_for_graph — metadata-only (schema_only=True, sources scan limit=0) resolves relational columns; code ops in _UNTYPED (schema.py:17) return null`
- ⬜ **Error-rate gate (post-write error threshold)** — Scaffolded but dead: no error-rate is computed or thresholded anywhere (grep for error_rate/threshold finds only the label). The per-transform onError='skip'/'raise' is a separate, real feature.  `kernel/kernel/compiler.py:46 emits an '__error_gate__' PlanStep when a write exists, but kernel/kernel/plugins/runner.py:160 handles kind=='error_gate' with only `time.sleep(0.02)``
- 🟡 **Standalone loop node** — A bare 'loop' node performs no actual iteration in the engine — it is a pass-through placeholder. Real iterative control flow is done via `section` (maxRuns driver script). Cycles are rejected up front (must be encapsulated, §5.7, engine.py: is_acyclic checks).  `kernel/kernel/executors/engine.py:307 loop/opaque/write just pass through parent on full run and raise NotPreviewable otherwise; compiler maps it to kind='loop' (compiler.py:19)`
- ✅ **Branch routing (conditional edge routing)** — Empty predicate sends everything to true, `1=0` (empty) to false.  `kernel/kernel/executors/engine.py:322 _route_branch filters the input by predicate for the true port and NOT(predicate) for the false port; applied per outgoing edge in _inputs (engine.py:113)`
- ✅ **SSRF-safe object-store extension policy** — Prevents an arbitrary https:// uri from silently pulling httpfs and fetching it; s3://gs:// still work because credentials/httpfs are loaded deliberately. Security-relevant to the engine's source scanning.  `kernel/kernel/db.py:41 _apply_session disables autoinstall/autoload of extensions on base conn AND every per-run cursor (db.py:92); httpfs loaded only explicitly in ensure_object_store (db.py:124)`
- ✅ **Live status + run history persistence (cross-instance / restart-safe)** — Lets another stateless web instance or a restarted kernel answer a status poll for a run it didn't launch, instead of 404-ing. Subprocess child disables its own on_complete so the parent records once (subrun.py:34).  `runner.py:110 _emit fires on_status (DB run_states) each transition; runner.py:203/subprocess_runner.py:148 on_complete persists terminal run; routers/runs.py:184 _status_or_lost falls back to metadb.get_run_state then a synthetic terminal status`

## Data, Adapters & Catalog

- ✅ **DuckDB adapter — Parquet scan (lazy/out-of-core)** — Default reader; also handles object-store prefixes of parquet parts (adapters.py:139).  `kernel/kernel/plugins/adapters.py:151`
- ✅ **DuckDB adapter — CSV/TSV scan**  `kernel/kernel/plugins/adapters.py:144`
- ✅ **DuckDB adapter — JSON/NDJSON scan**  `kernel/kernel/plugins/adapters.py:146`
- 🟡 **DuckDB adapter — Arrow/Feather/IPC scan** — Local files only; feather is read via pyarrow into DuckDB. No object-store path for feather and no append support (write append rejects it, adapters.py:188).  `kernel/kernel/plugins/adapters.py:148`
- ✅ **Directory-of-files scan (part-*.<ext> dataset)** — Scans a dir written by append mode; covers parquet/pq/csv/tsv/json via recursive glob.  `kernel/kernel/plugins/adapters.py:153`
- ✅ **CSV parse options (delimiter / header override; else auto-detect)** — Source node passes overrides only when set (engine.py:164); delimiter accepts 'tab'/'\t'.  `kernel/kernel/plugins/adapters.py:61`
- ✅ **Lance streaming scan with column/limit pushdown** — Requires optional `lance` extra (not installed in this env; tests skip). Column/limit pushed to Lance scanner; predicate is applied post-scan in DuckDB, not pushed to Lance.  `kernel/kernel/plugins/adapters.py:246`
- ✅ **Object-store scan/write (s3:// gs:// gcs:// r2://) via DuckDB httpfs** — httpfs loaded explicitly in db.ensure_object_store (db.py:124). Object-store test passes with a mocked bucket (test_kernel.py:909).  `kernel/kernel/plugins/adapters.py:130`
- ✅ **Object-store credentials (explicit keys / AWS chain / custom endpoint for MinIO/R2)** — CREATE SECRET from `objectStore` setting; falls back to credential_chain. Endpoint/USE_SSL/URL_STYLE handled for S3-compatible hosts.  `kernel/kernel/db.py:138`
- ✅ **Catalog list / get / search** — In-memory, RLock-serialized; search matches name or uri substring.  `kernel/kernel/plugins/catalog.py:101`
- ✅ **Catalog lineage (connected-component graph around a uri)** — BFS over dedup'd parent/child edges; merges edges from other instances before traversal.  `kernel/kernel/plugins/catalog.py:122`
- ✅ **Catalog seed from local data directory on startup** — Discovers parquet/csv/tsv/json/arrow files and .lance dirs.  `kernel/kernel/plugins/catalog.py:39`
- ✅ **Catalog register (user-adds a dataset, persisted across restart)** — Validates readability via adapter.schema; saves to `datasets` global setting so it re-registers on restart (test_kernel.py:1387).  `kernel/kernel/routers/catalog.py:75`
- ✅ **register_output (write nodes register outputs + lineage edges)** — Runner commits write then registers output with parent uris and pipeline='canvas'.  `kernel/kernel/plugins/runner.py:253`
- ✅ **Catalog DB-backing / cross-instance (catalog_entries + catalog_edges)** — Write-through on _persist/_add_edge; merge-on-read via _load_from_db so a dataset another stateless instance registered becomes visible (test_kernel.py:1149). Best-effort (DB errors swallowed); every list/get/lineage does a full re-merge from DB — no incremental sync, a scaling concern at large catalogs.  `kernel/kernel/plugins/catalog.py:82`
- ✅ **Write formats parquet / csv / tsv / json / arrow / lance** — Format chosen by extension; JSON via COPY ... (FORMAT JSON, ARRAY true); Lance via streamed record batches. Round-trip test at test_kernel.py:405.  `kernel/kernel/plugins/adapters.py:207`
- ✅ **Write mode overwrite**  `kernel/kernel/plugins/adapters.py:199`
- 🟡 **Write mode append (directory of part files)** — Only parquet/csv/tsv (and Lance native append); JSON/Arrow/Feather raise NotImplementedError. Each run adds a part file; reader globs them back (test_kernel.py:386).  `kernel/kernel/plugins/adapters.py:185`
- 🟡 **Atomic overwrite (temp file + os.replace)** — Atomic only for LOCAL writes — a failed/cancelled write never truncates the existing dataset (test_kernel.py:438). Object-store overwrite writes in place (relies on single-object PUT atomicity; a multi-file/dir object-store overwrite is not transactional).  `kernel/kernel/plugins/adapters.py:203`
- ✅ **Content-addressed write skip (idempotent overwrite re-run)** — Skips re-writing when an identical overwrite plan already produced the output and it still exists; append is never cached.  `kernel/kernel/plugins/runner.py:218`
- ✅ **Destinations — local backend browse + mkdir (root-confined)** — realpath check blocks traversal outside the destination root; .lance dirs shown as files.  `kernel/kernel/destinations.py:27`
- 🟡 **Destinations — object-store backend browse + target_uri** — Browses a prefix via glob(); mkdir is a no-op for object stores (no real folders — prefix created on write, destinations.py:150). Browse errors surface honestly when creds/bucket missing.  `kernel/kernel/destinations.py:61`
- ✅ **Destination presets (global setting + default workspace outputs)** — Default 'outputs' place always injected; DP_STORAGE_URL can make it an s3/gs prefix.  `kernel/kernel/destinations.py:110`
- ✅ **Vector search — Lance native ANN (uses vector index if present)** — Only when input is a bare .lance source; requires `lance` extra; falls back to brute-force cosine on any error. Exposes _score = 1 - cosine distance (adapters.py:257).  `kernel/kernel/executors/engine.py:447`
- ✅ **Vector search — brute-force cosine over DuckDB** — list_cosine_similarity ORDER BY DESC LIMIT k; requires a fixed-size numeric list/array column. Runs full (unsampled) input even in preview for faithful top-K.  `kernel/kernel/executors/engine.py:453`
- ✅ **Vector search — query by external vector or by row index** — queryVector accepts JSON string or list; else uses queryRow offset into the dataset.  `kernel/kernel/executors/engine.py:428`
- ✅ **SSRF guard — DuckDB extension autoload/autoinstall disabled** — Blocks an arbitrary https:// uri from silently pulling httpfs and fetching remote data; re-asserted on every per-run cursor. Object-store access loads httpfs explicitly.  `kernel/kernel/db.py:42`
- ✅ **CORS restricted to localhost origins** — Origin regex allows only localhost/127.0.0.1.  `kernel/kernel/main.py:38`
- ⬜ **Dataset URI confinement for local paths** — GAP: /catalog/register and source nodes accept any absolute local path (abspath+expanduser, no root allowlist); the engine scans it directly (engine.py:158). Only the destinations browse surface is root-confined — dataset READ is not, so an authed user can read arbitrary local files. SSRF over http(s) is blocked, but local-FS confinement is absent.  `kernel/kernel/routers/catalog.py:79`
- ✅ **mem:// adapter (in-memory named tables)** — Used to expose in-process DuckDB tables as datasets (mainly tests/fixtures).  `kernel/kernel/plugins/adapters.py:128`

## Collaboration, Multi-user & Auth

- ✅ **Live co-editing (Yjs CRDT)** — Merge is real CRDT (nodes/edges/meta maps), not last-write-wins. Server is a dumb relay with NO server-side Y.Doc: convergence depends on peer-to-peer ysync handshake. 'First in room' is decided by an 800ms timer (ydoc.ts hydrateIfEmpty), a heuristic that can race under slow peer joins.  `web/src/collab/ydoc.ts:50-113 (store<->Y.Doc bidirectional bridge, per-field node diffing so a drag never clobbers a config edit); kernel/kernel/tests/test_kernel.py:1164 test_collab_relay_gates_viewer_doc_updates passes`
- ✅ **Presence / peer cursors** — Cursor color is random per browser session (collab.ts:9-10), not tied to user identity.  `web/src/collab/collab.ts:92-96 sendCursor throttled to 50ms; web/src/canvas/PeerCursors.tsx maps flow-coords to each viewer's screen via viewport; kernel test_collab_relay_broadcasts_and_leave (test_kernel.py:1054)`
- 🟡 **Collab WebSocket relay (per-canvas room)** — Per-instance in-memory only. Multi-instance/stateless-web deployments need sticky routing of each canvas to one instance (acknowledged in main.py:11-13 docstring); peers on different instances will NOT see each other. Fine for the default single-process deployment.  `kernel/kernel/main.py:88-135 _collab_rooms is an in-memory dict[canvas_id -> set[WebSocket]]`
- ✅ **Viewer-role write-gating on the collab channel** — Closes the laundering path where an editor peer would otherwise merge+autosave a viewer's edit past the read-only boundary.  `kernel/kernel/main.py:115-116 drops inbound 'yjs' doc updates from a viewer while still relaying its presence; test_collab_relay_gates_viewer_doc_updates (test_kernel.py:1164) confirms yjs dropped, presence + editor edits relayed`
- ✅ **Collab WebSocket auth gating** — In open mode (no DP_AUTH_SECRET) the ws is ungated and everyone may edit — by design for single-user/trusted instances.  `kernel/kernel/main.py:97-103 verifies dp_session cookie + canvas_role, closes 1008 if no role; test_collab_ws_requires_auth_when_enabled (test_kernel.py:1065)`
- ✅ **Run-status WebSocket auth gating**  `kernel/kernel/main.py:69-71 closes 1008 without a valid session; test_run_ws_requires_auth_when_enabled (test_kernel.py:1079)`
- ✅ **Per-user identity (scrypt passwords + signed session cookies)** — Password stored as scrypt$salt$hash (stdlib only). DP_AUTH_PASSWORD is bootstrap-only (seeds default user hash on first init, metadb.py:186-190). Cookie is httponly+samesite=lax; Secure flag is opt-in via DP_AUTH_SECURE_COOKIE (off by default for internal http installs). No CSRF token beyond samesite=lax.  `kernel/kernel/auth.py:66-83 salted scrypt hash + constant-time verify; auth.py:38-60 HMAC-signed time-limited token (7-day TTL); routers/workspace.py:31-43 /auth/login verifies THIS user's own hash; test_per_user_password_is_not_a_skeleton_key (test_kernel.py:673) and test_signed_session_auth (test_kernel.py:1345)`
- ✅ **Self-service password change/rotation**  `kernel/kernel/routers/workspace.py:46-56 /auth/password requires old password if one is set, min 6 chars; web/src/views/Shell.tsx:72-119 change-password dialog; rotation asserted in test_kernel.py:688-694`
- ✅ **X-DP-User open (no-auth) dev mode** — By design this trusts any header value = that user with no verification; only safe for single-user/trusted deployments. When auth is enabled the header is explicitly NOT trusted (security.py:21-25, asserted test_kernel.py:1364).  `kernel/kernel/security.py:21-26 current_user falls back to X-DP-User header when DP_AUTH_SECRET unset; metadb.resolve_user defaults to seeded 'local' user (metadb.py:208-216)`
- ✅ **Secure-default /api auth gate (router-level Depends)** — Gate is applied at include time, so a newly added route is protected unless explicitly placed on public_router.  `kernel/kernel/main.py:50-54 public_router mounted WITHOUT gate, catalog/runs/workspace routers mounted WITH Depends(current_user); test_api_routes_require_auth_when_enabled (test_kernel.py:697) proves /run, /data, POST /users, /catalog, /canvas, /settings all 401 unauth while /auth/status + GET /users stay public`
- ✅ **Login screen + login roster** — User picker + password. No SSO/OIDC (auth.py:12 notes it 'would slot in later' — not implemented).  `web/src/views/Login.tsx:10-21 login form; web/src/App.tsx:38 renders Login when authEnabled && !userId; public GET /users returns id+name only, no emails (workspace.py:81-84, asserted test_kernel.py:719)`
- ✅ **Admin user creation** — No self-registration by design; there is no role/admin distinction — any authenticated user can create users (POST /users is only gated by current_user, not an admin check).  `kernel/kernel/routers/workspace.py:87-94 POST /users (gated — no anonymous self-registration), sets scrypt hash if password provided`
- 🟡 **Canvas sharing — visibility (private/workspace) + explicit collaborators (editor/viewer)** — Gap: 'workspace' visibility grants EDITOR to every user on the instance (metadb.py:248-249) — there is no workspace read-only tier. Explicit per-user viewer role exists, but you cannot make a canvas workspace-visible read-only.  `kernel/kernel/routers/workspace.py:197-213 owner-only add/remove share + set visibility; metadb.canvas_role (metadb.py:240-251); web/src/panels/ShareModal.tsx; test_canvas_sharing_access_and_authz (test_kernel.py:1368)`
- ✅ **Canvas access control / authz (owner/editor/viewer roles)** — Frontend shows a 'view-only' toast + accessDenied state on a 403 save instead of faking sync (graph.ts:887-891).  `metadb.canvas_role returns owner\|editor\|viewer\|None (metadb.py:240-251); put_canvas 403s non-editors (workspace.py:129-147); delete is owner-only (workspace.py:180-184); editor cannot re-share (workspace.py:199, asserted test_kernel.py:1385)`
- ✅ **Autosave (debounced PUT) + offline cache**  `web/src/store/graph.ts:859-898 400ms-debounced saveCanvas PUT + unconditional localStorage cache; distinguishes 403 (view-only) from offline (graph.ts:886-895)`
- ✅ **Collab write-amplification guard** — Prevents N co-editors each PUTting the whole doc on every keystroke (N-way amplification). The offline cache write is still unconditional so a reload keeps merged peer edits.  `web/src/collab/undo.ts:23 collabApply.remote flag; graph.ts:870-873 skips PUT for a peer's merged edit (only the originating editor PUTs); local edits + undo/redo still PUT`
- ✅ **pagehide flush on tab close** — Closes the collab edge case where the sole originating editor disconnects inside the 400ms debounce window.  `web/src/store/graph.ts:906-911 pagehide listener PUTs with keepalive only when saved===false`
- ✅ **Canvas version history + restore** — Restore is itself undoable. Autosave fires ~400ms but snapshotter throttles to 90s, so history is coarse-grained by design.  `kernel/kernel/metadb.py:422-461 snapshot_canvas (throttled 90s, dedup, prune to newest 30; named snapshots always kept) + list_versions + get_version_doc; workspace.py:150-177 versions/restore endpoints (restore snapshots pre-restore state as 'before restore'); web/src/panels/VersionHistoryModal.tsx; test_canvas_version_history_and_restore (test_kernel.py:737)`
- ✅ **Run history (per-canvas, persisted)** — No-op for ad-hoc/unsaved-canvas runs (metadb.py:310-314).  `kernel/kernel/metadb.py:306-340 record_run + list_runs (RunRecord table, survives restart); workspace.py:216-221 GET /canvas/{id}/runs with same authz; web/src/panels/RunHistoryModal.tsx`
- ✅ **CRDT-aware collaborative undo/redo** — Undo reverts only your own changes and won't delete a node a peer added concurrently (the old full-doc snapshot undo did).  `web/src/collab/ydoc.ts:99-104 Y.UndoManager tracks only origin 'store' (local edits); web/src/collab/undo.ts crdtUndo indirection; store falls back to its own snapshot stack when offline (ydoc.ts:134)`
- ✅ **Secrets redaction in settings API** — Not a collab/auth feature per se but part of the authed workspace surface; secrets never leave the kernel in plaintext.  `kernel/kernel/routers/workspace.py:226-262 GET redacts agentApiKey + objectStore access keys to __redacted__ sentinel; PUT treats the sentinel as 'keep stored' so dots never overwrite a real secret`
- ✅ **CORS restricted to localhost** — Assumes local/same-origin deployment; a real remote HTTPS deployment behind a domain would need this widened.  `kernel/kernel/main.py:41-45 allow_origin_regex localhost/127.0.0.1 only (no wildcard), so a random site the user visits can't read the local API cross-origin`

## Extensibility & Plugin SPI

- ✅ **Plugin discovery — drop-in workspace folder** — Scans <workspace>/plugins/<pack>/ for packages with register(reg), adds dir to sys.path, reads dataplay.toml first. Covered by test_plugin_version_negotiation (test_kernel.py:1237).  `kernel/kernel/deps.py:150-161`
- ✅ **Plugin discovery — pip entry-points (dataplay.plugins group)** — Loads entry_points(group='dataplay.plugins') and calls ep.load()(reg). Real path but NO test coverage, and min_core_api negotiation is NOT applied to entry-points (only to drop-in packs).  `kernel/kernel/deps.py:165-174`
- ✅ **Plugin discovery — DP_PLUGINS env module list** — Comma-separated module names imported and register(reg) called via _register_module.  `kernel/kernel/settings.py:20, kernel/kernel/deps.py:163-164`
- ✅ **Node-authoring SDK — add_node + ctx builders (sql/arrow_map/polars)** — ctx.sql uses a {input} placeholder view; arrow_map/polars are lazy relation->relation helpers. add_node refuses to shadow a built-in or already-registered plugin kind (deps.py:44-49).  `kernel/kernel/sdk.py:38-70, kernel/kernel/deps.py:39-52`
- ✅ **NodeLowering protocol (plugin lowering contract)** — runtime_checkable Protocol; engine dispatches plugin kinds via deps.node_lowerings. Single-output returns Relation, multi-output returns {port_id: Relation}.  `kernel/kernel/backends.py:23-36`
- 🟡 **NodeSpec generic frontend rendering (plugin nodes need no frontend code)** — Plugin nodes render generically with typed ports/params from /api/nodes. GAP: NodeParamFields filters out type==='code' params, and GenericNode renders only NodeParamFields — so a plugin node declaring a code param (python/sql) gets NO editor in the generic card; only hand-built cards render code editors.  `web/src/nodes/generic.tsx:97-119, generic.tsx:39`
- ✅ **Front/back node-spec parity guard** — Test parses each web/src/nodes/kinds/*.tsx register({...}) literal and compares ports/wire/accepts against BUILTIN_NODE_SPECS, guarding drift for kinds that have a hand-built card. Kinds rendered generically are skipped (by design).  `kernel/kernel/tests/test_kernel.py:1263-1334`
- ✅ **Dataset-adapter SPI (add_adapter; DuckDB + Lance built-ins)** — DuckDBAdapter (parquet/csv/json/arrow/dir/object-store) + LanceAdapter. Full matches/scan/schema/count/fingerprint/write contract. Plugins insert(0) so they claim URIs before defaults; resolve_adapter falls back to DuckDBAdapter.  `kernel/kernel/plugins/adapters.py:99-291, kernel/kernel/deps.py:54-55,126-133`
- ✅ **Lance native ANN (optional adapter.nearest)** — LanceAdapter.nearest pushes cosine kNN into Lance (index or flat scan), exposes _score=1-distance. The generic vector-search node falls back to brute-force cosine when the adapter lacks nearest.  `kernel/kernel/plugins/adapters.py:257-264`
- ✅ **ExecutionBackend SPI (add_runner; alternate runners)** — runtime_checkable Protocol (name/can_run/estimate/run/status/cancel). Two real backends ship: LocalRunner + SubprocessRunner (subprocess_runner.py:28-160). pick_runner honors Settings->Execution 'backend' choice by name then first can_run; run_index routes status/cancel to the owning runner (capped at 1000).  `kernel/kernel/backends.py:39-57, kernel/kernel/deps.py:57-59,135-147, kernel/kernel/routers/runs.py:165-181`
- 🟡 **Capabilities — media/vector column tagging + viewer tabs** — Media/vector detection works (regex over name/type in tag_columns) and the Media viewer grid renders. GAPS: (1) reg.add_capability only announces an id+label to KernelInfo — a plugin CANNOT add new column detection (tag_columns is hardcoded core) nor a viewer tab (that's a separate frontend registration), as the module docstring itself admits (capabilities.py:47-51). (2) The Vector viewer tab was removed (capabilities.tsx:49-50); vectors only get an inline cell chip.  `kernel/kernel/plugins/capabilities.py:35-51, web/src/nodes/capabilities.tsx:42-50`
- ✅ **Processor library + promote-to-library** — Registry ships EMPTY by design. promote() registers a code-backed processor from an ad-hoc cell with auto-incrementing version; plugins register fn_factory-backed processors. POST /processors/promote + GET /processors wired. build() compiles code via the same sandbox as ad-hoc.  `kernel/kernel/plugins/processors.py:62-97, kernel/kernel/routers/catalog.py:150-157`
- ✅ **Catalog provider SPI (set_catalog; InMemoryCatalog default)** — Registry.set_catalog swaps the provider. Default InMemoryCatalog is a per-instance cache with write-through + load from the shared metadb (catalog_entries/edges) so stateless web instances stay consistent; thread-safe via RLock; grows lineage as write nodes register outputs.  `kernel/kernel/plugins/catalog.py:21-177, kernel/kernel/deps.py:67-68,105`
- ✅ **Plugin version negotiation (min_core_api vs CORE_API_VERSION)** — _read_manifest validates dataplay.toml (name/version required, optional min_core_api). A pack requiring a newer core is recorded as an error and NOT loaded; missing manifest loads unversioned; malformed manifest blocks. NOTE: only enforced for drop-in packs — entry-point and DP_PLUGINS modules bypass this check entirely.  `kernel/kernel/deps.py:30,176-201, kernel/kernel/tests/test_kernel.py:1237-1260`
- ⬜ **Pipeline importer SPI (/pipelines/import)** — DEAD SCAFFOLDING. NullImporter exists but is never instantiated; Deps has no `importer` attribute and Registry has no set_importer, so a plugin cannot register an importer. Verified empirically: get_deps().importer raises AttributeError, which the endpoint's generic `except Exception` turns into HTTP 400 ('AttributeError: Deps object has no attribute importer') instead of the intended 501 ImporterNotConfigured. The endpoint is broken for every input.  `kernel/kernel/plugins/importer.py:19-27, kernel/kernel/deps.py (no self.importer), kernel/kernel/routers/catalog.py:121-128`
- ✅ **Plugin introspection endpoint (/api/plugins, /api/kernel)** — GET /api/plugins lists loaded packs with source/version/error; KernelInfo reports adapters/runners/processors/capabilities. Load failures are captured (name+error+traceback tail) rather than crashing startup (deps.py:210-214).  `kernel/kernel/routers/catalog.py:46-48, kernel/kernel/deps.py:216-223`

## Platform, Design System & Ops

- ✅ **dataplay one-command launcher / CLI** — Argparse CLI: --host/--port/--workspace/--data-dir/--no-open/--no-seed; seeds sample data, configures workspace before deps build, runs uvicorn, opens browser.  `kernel/kernel/cli.py:15-44; pyproject.toml [project.scripts] dataplay="kernel.cli:main"`
- ✅ **FastAPI app factory + router split** — Routes split across catalog/runs/workspace routers; a separate public_router (auth status/login/logout/roster) is mounted WITHOUT the gate, everything else WITH Depends(current_user).  `kernel/kernel/main.py:37-54; routers/{catalog,runs,workspace}.py`
- ✅ **Secure-default auth gate on all /api routes** — Whole /api router gated at include time; a new route is protected unless explicitly added to public_router. Fixes a prior era where /run,/data,/catalog were wide open.  `kernel/kernel/main.py:50-54; kernel/kernel/security.py:16-26`
- ✅ **SPA serving (single process, bundled + dev fallback)** — Serves kernel/_web (wheel bundle) if present, else web/dist. force-include of ../web/dist in pyproject wheel target. No SPA-history fallback route, but StaticFiles(html=True) covers index.  `kernel/kernel/main.py:145-149`
- ✅ **CORS restricted to localhost origins** — allow_origin_regex localhost/127.0.0.1 only (deliberately not wildcard). Same-origin SPA + Vite proxy; a cross-domain deployment behind an LB would need this widened.  `kernel/kernel/main.py:41-45`
- ✅ **Per-user password auth (scrypt + signed session cookies)** — HMAC-signed, week-TTL session tokens; per-user salted scrypt hash; DP_AUTH_PASSWORD only bootstraps the default user; login screen, logout, and change-password UI all present. Opt-in via DP_AUTH_SECRET; open X-DP-User dev mode otherwise.  `kernel/kernel/auth.py:38-83; routers/workspace.py:31-56; web/src/views/Login.tsx; web/src/views/Shell.tsx:72-119`
- ✅ **Secret redaction in settings API** — GET redacts agentApiKey + objectStore.{accessKeyId,secretAccessKey} to a sentinel; PUT treats the sentinel as 'unchanged' so dots never overwrite a stored secret.  `kernel/kernel/routers/workspace.py:226-262`
- ✅ **Global settings page (agent / execution / destinations)** — Full-screen settings with left nav. Covers agent model/key/baseURL, execution runner picker, destinations + object-store creds.  `web/src/panels/SettingsModal.tsx:23-154; routers/workspace.py:238-262`
- 🟡 **User-scoped settings** — DB schema + GET/PUT support scope='user', but SettingsModal only reads/writes global (state seeded from s.global only, line 33). No user-preference UI surface beyond theme (which is localStorage, not the DB).  `kernel/kernel/metadb.py:132-140,464-470; routers/workspace.py:246-247`
- ✅ **Metadata DB (SQLAlchemy, SQLite dev / Postgres prod)** — Users, canvases, shares, run records/states, versions, catalog entries/edges, settings. Only the connection string (DP_DATABASE_URL) is config.  `kernel/kernel/metadb.py:39-154; settings.py:17-18; pyproject.toml postgres extra`
- ✅ **Alembic migrations (with legacy-DB adoption)** — 7 revisions, each with upgrade+downgrade; init_db stamps 0001_baseline for pre-Alembic DBs then upgrades to head. Alembic is the schema source of truth (not create_all).  `kernel/kernel/metadb.py:167-191; migrations/versions/0001..0007`
- ✅ **Light/dark theme toggle (system-follow, no-flash)** — light/dark/system modes, localStorage-persisted, stamps data-theme on <html>, follows OS in system mode, applied before first paint. Toggle in TopBar.  `web/src/theme/mode.ts:9-43; web/src/main.tsx:7; web/src/canvas/TopBar.tsx:205; tailwind.config.js darkMode:['class','[data-theme="dark"]']`
- ✅ **Design tokens (shadcn HSL vars + TS mirror)** — shadcn HSL triples in :root + [data-theme='dark']; tokens.ts mirrors them (resolving to var()) for inline-styled components. Dual system (shadcn tokens + legacy vars) is a deliberate, documented migration state.  `web/src/index.css:11-107; web/src/theme/tokens.ts:8-99; tailwind.config.js colors map`
- 🟡 **shadcn/ui migration** — Radix+cva shadcn primitives exist and newer panels use them, but the migration is explicitly incomplete — many inline-styled/legacy-var components remain. index.css:4 says 'Preflight is disabled … mid-migration' while tailwind.config.js says 'Preflight ON' — a stale, contradictory comment.  `web/src/components/ui/*.tsx (button,dialog,input,select,…); index.css:4 vs tailwind.config.js Preflight comment`
- ✅ **LLM agent — provider-agnostic, in-process, server-side key** — Pydantic AI tool-use loop (add/connect/set_config/preview nodes) on a working graph; model chosen via DP_AGENT_MODEL or DB settings; key stays in kernel; request-limit bounds the loop; auto-layout of new nodes. NOTE agent.py header contradicts README: it uses Pydantic AI in-process, README/settings still describe a 'LiteLLM tool-use loop' (LiteLLM is only used for key detection).  `kernel/kernel/agent.py:99-257; routers/runs.py:110-124`
- ⬜ **Agent offline keyword-planner fallback** — README claims 'Without a configured model it falls back to a built-in offline keyword planner … The dock shows which is active.' The code does the opposite: AgentDock.tsx:13 states 'no rule-based stand-in that pretends to be an LLM' and simply shows 'Agent unavailable'. The advertised offline planner does not exist.  `web/src/panels/AgentDock.tsx:13,50-51; README.md:151-152`
- ✅ **Stateless-web readiness — shared run state** — RunState table mirrors every status transition (on_status hook); GET /run/{id} + status WS answerable from any instance and survive restart.  `kernel/kernel/metadb.py:97-108,343-363; deps.py:78-83,115`
- ✅ **Stateless-web readiness — shared catalog write-through** — catalog_entries/catalog_edges persist registered datasets, written outputs, and lineage; in-memory catalog is a per-instance cache that write-throughs + loads from the DB.  `kernel/kernel/metadb.py:110-129,366-398; deps.py:104-110; migration 0007`
- ⬜ **Stateless-web readiness — distributed execution** — Execution always runs on the accepting instance. reconcile_orphaned_runs assumes a SINGLE execution instance — on startup it marks ALL non-terminal runs failed, so in a multi-instance deploy one instance's restart would cancel another's live runs. Needs per-run instance ownership + heartbeat + an ExecutionBackend plugin. Documented as a known gap.  `kernel/kernel/metadb.py:401-419 (reconcile_orphaned_runs TODO); README.md:95-99`
- 🟡 **Realtime collaboration (per-canvas room)** — In-memory broadcast relay per canvas with presence + role gating (viewer can't relay yjs edits). Last-write-wins, not conflict-free (Yjs/CRDT is dep-present but noted as future hardening). Rooms are process-local → deployment must sticky-route /ws/collab/{canvas_id} to one instance (no server-side coordination).  `kernel/kernel/main.py:88-135`
- ✅ **Canvas version snapshots + restore** — Throttled auto-snapshots (dedup, 90s, keep 30) + named snapshots on save; restore endpoint snapshots current state first so restore is itself undoable.  `kernel/kernel/metadb.py:84-94,422-461; routers/workspace.py:150-177`
- ✅ **Startup reconcile of orphaned runs** — Runs left queued/running by a stopped kernel are marked failed('interrupted') so clients don't poll forever. Correct for single-instance; unsafe for multi-instance (see distributed-execution gap).  `kernel/kernel/metadb.py:191,401-419`
- ✅ **Offline / zero-config operation** — No cloud account/external service; SQLite bundled; sample data seeded on first run; engine deps (DuckDB/Polars/Arrow, optional Lance) all local. Agent + Postgres + Lance are opt-in extras.  `README.md:8-9,26; kernel/kernel/settings.py:13-18; pyproject.toml core deps`
- 🟡 **User management (create users)** — POST /users exists (gated, no anon self-registration) and GET /users powers the login roster, but there is NO admin UI to create users or set their passwords — must be done via API/script. Only self change-password is surfaced.  `kernel/kernel/routers/workspace.py:81-94; web/src/api/client.ts:131`
- 🟡 **Deployment documentation** — README covers the stateless-web model, DP_DATABASE_URL→Postgres, object storage, and an nginx consistent-hash example for collab. No dedicated deployment guide, no Dockerfile/compose, no reverse-proxy/TLS/systemd runbook.  `README.md:77-99 ('Scaling out'); no docs/DEPLOY*.md (docs/ has PRD, TESTING, CONTROL_FLOW only)`
- ✅ **Plugin discovery & SPI (nodes/adapters/runners/capabilities/catalog)** — Two discovery paths (drop-in <workspace>/plugins/<pack>/ + pip entry points); manifest (dataplay.toml) validation with min_core_api gate; refuses to shadow built-in or already-registered kinds. Errors recorded (surfaced via GET /plugins) instead of crashing.  `kernel/kernel/deps.py:33-215; kernel/README.md`
- 🟡 **Pluggable execution backend selection** — pick_runner honors the 'backend' setting; ships LocalRunner + a real SubprocessRunner. UI runner picker works, but pod/Ray/queue runners are only a documented plugin extension point, not shipped — so 'dedicated execution tier' is scaffolding for now.  `kernel/kernel/deps.py:116-147; SettingsModal.tsx:95-104`

